// Base-Imports deines Clients
import me.alpha432.oyvey.OyVey;
import me.alpha432.oyvey.event.impl.entity.player.TickEvent;
import me.alpha432.oyvey.event.system.Subscribe;
import me.alpha432.oyvey.features.modules.Module;
import me.alpha432.oyvey.features.settings.Setting;
import me.alpha432.oyvey.util.models.Angles;

public class AimAssistModule extends Module {

    public AimAssistModule() {
        // Ruft den Konstruktor der Klasse 'Module' auf
        // und übergibt Name, Beschreibung und Kategorie
        super("AimAssist", "Eine Beschreibung", Category.COMBAT);
    }
}

// Minecraft (Yarn / Official Mapping) Imports
import net.minecraft.world.entity.Entity;
import net.minecraft.world.entity.player.Player;

EntityPlayer target = null;
double closestDistance = range; // z.B. 4.5 Blöcke

for (EntityPlayer player : mc.world.playerEntities) {
    if (player == mc.player || player.isDead || player.getHealth() <= 0) continue;
    
    double distance = mc.player.getDistance(player);
    if (distance < closestDistance) {
        closestDistance = distance;
        target = player;
    }
}
